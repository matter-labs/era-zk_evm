[package]
name = "zk_evm"
version = "1.5.0"
edition = "2021"
authors = ["The Matter Labs Team <hello@matterlabs.dev>"]

resolver = "2"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
num = { version = "0.4"}
static_assertions = "1"

# TODO: change to use matter-labs when everything is merged:
# zk_evm_abstractions = { git = "https://github.com/matter-labs/era-zk_evm_abstractions.git", branch = "dl-precompiles" }
zk_evm_abstractions = { git = "https://github.com/distributed-lab/era-zk_evm_abstractions.git", branch = "feature/ec-and-modexp-precompiles" }
# zk_evm_abstractions = { path = "../era-zk_evm_abstractions" }

lazy_static = "1.4"

[dev-dependencies]
hex = "0.4"

[features]
